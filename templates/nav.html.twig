<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('home') }}">
            <img src="https://ik.imagekit.io/bellyne/Symfony/icone_112_gb0QQ74gnj.png?ik-sdk-version=javascript-1.4.3&updatedAt=1647878599088" alt="" width="30" height="24">
          </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link"href="/">Home</a>
          </li>
          {% if is_granted("ROLE_USER") %}
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Articles
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            
              <li><a class="dropdown-item" href="{{ path('create_post') }}">Ajouter</a></li>
            
              {# <li><a class="dropdown-item" href="/post">Index</a></li> #}
            </ul>
          </li>
          {% endif %}
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Categories
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              {% if is_granted("ROLE_USER") %}
              <li><a class="dropdown-item" href="{{ path('create_category') }}">Ajouter</a></li>
              {% endif %}
              <li><a class="dropdown-item" href="{{ path('index_category') }}">Index</a></li>
            </ul>
          </li>
        </ul>
      </div>
       <form class="d-flex" action="{{ path('search_in_post') }}">
        <input class="form-control me-sm-2" type="text" name="search" placeholder="Search">
        <button class="btn btn-secondary my-2 my-sm-0" type="submit">Rechercher</button>
      </form>
      <div class="d-flex">
        {% if app.user %}
          <p class="text-warning me-3 mt-2">Bonjour {{ app.user.username }}</p>
          <a href="{{ path('logout')}}"><button type="button" class="btn btn-danger">Déconnexion</button></a>
        {% else %}
          <a href="{{ path('app_register') }}"><button type="button" class="btn btn-warning">Inscription</button></a>
          <a href="{{ path('login') }}"><button type="button" class="btn btn-success">Connexion</button></a>
        {% endif %}
      </div>
    </div>
  </nav>